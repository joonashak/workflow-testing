name: Trigger by release

on:
  workflow_run:
    workflows:
      - Release
    types:
      - completed

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"
      - run: This workflow was trigger by an auto-generated release.
      - run: Status was ${{ github.event.workflow_run.conclusion }}
